import {useMemo,useState} from "react";import {Gift,Search,Ticket,WalletCards,X} from "lucide-react";import {rewards} from "../data/data";import {fmt} from "../hooks/useAppState";
export default function Rewards({state,setState,T,toast}){const[q,setQ]=useState(""),[sel,setSel]=useState(null);const list=useMemo(()=>rewards.filter(r=>`${r.title} ${r.subtitle}`.toLowerCase().includes(q.toLowerCase())),[q]);function buy(r){if(state.coins<r.cost){toast("Eco-Coin yetarli emas.","error");return}const code=`ECO-${Math.random().toString(36).slice(2,7).toUpperCase()}`;setState(s=>({...s,coins:s.coins-r.cost,coupons:[...s.coupons,{...r,code}]}));setSel(null);toast(`${r.title} kuponi olindi!`)}return <div className="page"><header className="page-head"><div><span>ECO-CATALOG</span><h2>{T.rewards}</h2></div><div className="wallet"><WalletCards/> {fmt(state.coins)} ✦</div></header><div className="search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder={`${T.search}...`}/></div><div className="reward-grid">{list.map(r=><button className="reward" key={r.id} onClick={()=>setSel(r)}><div className="reward-art"><span>{r.icon}</span><i>{r.tag}</i></div><b>{r.title}</b><small>{r.subtitle}</small><strong>{r.cost} ✦ <em>Olish</em></strong></button>)}</div><div className="section-title"><div><span>MY WALLET</span><h2>Kuponlar</h2></div></div>{state.coupons.length?state.coupons.map(c=><div className="coupon" key={c.code}><Ticket/><div><b>{c.title}</b><small>{c.code} • faol</small></div></div>):<div className="empty"><Gift/><b>Hali kupon yo‘q</b><small>Eco-Coin yig‘ib reward oling.</small></div>}{sel&&<div className="sheet-bg" onClick={()=>setSel(null)}><div className="sheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setSel(null)}><X/></button><div className="reward-big">{sel.icon}</div><h3>{sel.title}</h3><p>{sel.subtitle}</p><strong className="price">{sel.cost} ✦</strong><button className="primary-btn wide" onClick={()=>buy(sel)}>Rewardni olish</button></div></div>}</div>}
