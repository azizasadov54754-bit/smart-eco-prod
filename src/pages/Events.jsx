import {CalendarDays,Clock3,MapPin,Plus} from "lucide-react";import {events} from "../data/data";
export default function Events({toast,T}){return <div className="page"><header className="page-head"><div><span>COMMUNITY</span><h2>{T.events}</h2></div><button className="icon-btn" onClick={()=>toast("Tadbir yaratish admin panel orqali yoqiladi.","info")}><Plus/></button></header><div className="event-hero"><CalendarDays/><div><span>GREEN CAMPUS CALENDAR</span><b>Atrof-muhitni o‘zgartiradigan odamlar bilan uchrashing.</b></div></div><div className="events">{events.map(e=><article className="event" key={e.id}><div className="event-date"><b>{e.date.split(" ")[0]}</b><small>{e.date.split(" ")[1]}</small></div><div><b>{e.title}</b><small><Clock3/> {e.time} • <MapPin/> {e.place}</small></div><button onClick={()=>toast(`${e.title} kalendarga saqlandi!`)}>+</button></article>)}</div></div>}
