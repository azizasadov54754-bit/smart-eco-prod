import {Crown,Medal,Trophy} from "lucide-react";import {leaderboard} from "../data/data";
export default function Leader({state,T}){const rows=[...leaderboard,{name:state.user?.name||"Siz",coins:state.coins,badge:"You"}].sort((a,b)=>b.coins-a.coins);return <div className="page"><header className="page-head"><div><span>GREEN CAMPUS</span><h2>{T.leader}</h2></div><Trophy className="accent"/></header><div className="rank-banner"><Crown/><div><span>Campus ranking</span><b>Top Eco Community</b></div><strong>#{rows.findIndex(x=>x.name===(state.user?.name||"Siz"))+1}</strong></div><div className="leaders">{rows.map((r,i)=><div className={r.name===(state.user?.name||"Siz")?"leader you":"leader"} key={`${r.name}-${i}`}><span className="num">{i+1}</span><i>{r.name.split(" ").map(x=>x[0]).join("")}</i><div><b>{r.name}</b><small>{r.badge}</small></div><strong>{r.coins.toLocaleString()} ✦</strong>{i<3&&<Medal/>}</div>)}</div></div>}
